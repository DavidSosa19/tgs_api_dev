package com.example.tgs_dev.repository;

import com.example.tgs_dev.entity.ScheduleTemplateGroup;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ScheduleTemplateGroupRepository extends JpaRepository<ScheduleTemplateGroup, Long> {
}
